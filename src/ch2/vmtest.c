#include<stdio.h>

int main(void){
	// int型のhogeと256要素のbufを定義
	int hoge;
	char buf[256];

	print(f("&hoge..%p\n", &hoge);

	print(f("Input initial value.\n");
	fgets(buf, sizeof(buf), stdin);
	sscanf(buf, "%d", &hoge);

	for (;;) {
		print(f("hoge..%d\n", hoge);
		getchar();
		hoge++;
	}
	return 0;
}
